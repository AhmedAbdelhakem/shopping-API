import 'package:flutter/material.dart';

class EcommerceFavoriteScreen extends StatelessWidget {
  const EcommerceFavoriteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
